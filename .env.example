# Host port exposed by docker-compose (host:container => APP_PORT:8000)
APP_PORT=8000

# Comma-separated list of allowed CORS origins for browser clients
APP_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Whether CORS allows cookies/credentials
APP_CORS_ALLOW_CREDENTIALS=true

# API timeout used by Monte Carlo forecast logic (seconds)
APP_FORECAST_TIMEOUT_SECONDS=30
