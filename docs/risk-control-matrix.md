# Matrice risques–contrôles

Cette cartographie décrit les contrôles et preuves observables dans le dépôt au 1er août 2026. Elle distingue
la présence de code, l'existence d'un test et la maîtrise démontrée d'un risque. Elle ne constitue ni une
classification exhaustive du patrimoine de tests, ni une déclaration de conformité complète à
[`STD-TEST-001`](standards/STD-TEST-001.md). Le contrat cible des calculs et restitutions statistiques est
défini par [`STD-STAT-001`](standards/STD-STAT-001.md) ; son adoption documentaire ne prouve pas encore son
implémentation ni son contrôle bloquant.

## Méthode

- **Impact** : `Critique` (décision faussée, secret exposé, parcours essentiel ou gate compromis), `Élevé`
  (dégradation majeure ou exploitation incertaine), `Modéré` (effet circonscrit et récupérable) ou `Faible`.
- **Probabilité** : `Élevée` (condition déjà présente ou facilement déclenchable), `Moyenne` (condition
  plausible sans être systématique) ou `Faible` (plusieurs conditions ou garde-fous la rendent rare).
- **État de maîtrise** : `Couvert` seulement lorsque le contrôle et ses tests traitent le risque décrit,
  `Partiellement couvert` lorsqu'une preuve utile laisse une dimension non vérifiée, `Non couvert` en
  l'absence de preuve suffisante, et `Accepté formellement` uniquement avec une dérogation explicite et
  identifiable. Aucune dérogation de ce type n'a été trouvée.

Les appréciations d'impact et de probabilité sont qualitatives. Un test unitaire ou une ligne couverte ne
prouve pas à lui seul la sécurité, la résilience, la performance ou la compatibilité en conditions réelles.

## Matrice

| ID | Risque et justification | Conséquence | Impact | Probabilité | Contrôles actuels | Preuves actuelles | État de maîtrise | Parcours | Lacune résiduelle | PBI de traitement |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RISK-001 | Fuite du PAT ou du contexte Azure DevOps vers le backend. Critique car elle romprait l'invariant de confidentialité; faible car une gate dédiée inspecte payloads, modèles, persistance, routes et appels backend. | Secret ou identité Azure DevOps exposé côté serveur. | Critique | Faible | Appels ADO directs navigateur, modèles backend fermés, règles `IDENTITY-001` à `008`. | `Scripts/check_identity_boundary.py`; `tests/test_identity_boundary.py`; `frontend/src/hooks/simulationForecastCore.ts`; `backend/api_models.py` | Couvert | CP-001, CP-004 | Contrôle principalement statique et tests avec doubles; pas de preuve dynamique contre toute future chaîne de télémétrie. | PBI 6.5 |
| RISK-002 | Fuite du cookie `IDMontecarlo` vers Azure DevOps. Critique car elle corrélerait une identité locale avec ADO; faible car les appels ADO n'emploient pas les credentials navigateur et le cookie est créé pour le backend. | Corrélation ou divulgation de l'identifiant pseudonyme. | Critique | Faible | Cookie local, appels ADO directs sans transmission du cookie, couverture Vitals dédiée. | `frontend/src/clientId.ts`; `frontend/src/clientId.test.ts`; `frontend/src/adoClient.ts`; `frontend/tests/e2e/coverage.spec.js` | Couvert | CP-002, CP-004 | Le test E2E utilise des routes simulées; aucune observation réseau sur un vrai tenant ADO. | PBI 6.5 |
| RISK-003 | Divergence des calculs Python et TypeScript. Critique car deux chemins peuvent produire des décisions différentes; faible dans les versions et scénarios déclarés depuis l’enforcement complet de `main`. | Percentiles, dates ou capacités contradictoires selon le chemin. | Critique | Faible | Value Objects, standard, PRNG, ordre logique, corpus, sondes, rejeu exact et protocole multi-seeds sans moteur oracle. Le sous-DAG `main` valide les autorités, exécute les preuves spécialisées en parallèle, bloque compatibilité et statuts non conformes, puis valide le rapport consolidé depuis les mêmes artefacts attestés. | `docs/standards/STD-STAT-001.md`; `docs/statistical-reference-corpus.md`; `docs/statistical-distribution-protocol.md`; `docs/statistical-main-enforcement.md`; `config/statistical-main-enforcement-v1.0.json`; `contracts/statistical-compatibility-authority-v1.0.json`; `Scripts/statistical_main_enforcement.py`; `Scripts/run_statistical_exact_replay.py`; `Scripts/run_statistical_distribution.py`; `Scripts/run_statistical_compatibility.py`; `Scripts/generate_statistical_consolidated_report.py`; `reports/statistical-exact-replay-evidence.json`; `reports/statistical-distribution-evidence.json`; `reports/statistical-compatibility-evidence.json`; `reports/statistical-consolidated-report.json`; `tests/test_statistical_exact_replay.py`; `tests/test_statistical_main_enforcement.py`; `tests/test_statistical_main_enforcement_edges.py`; `frontend/src/statisticalDistributionRunner.test.ts` | Couvert | CP-003 | Les 80 comparaisons normatives et 64 comparaisons interlangages concordent exactement; les 16 cas sont indépendants des batches `125`, `128`, `1000` et `2048`, sans diagnostic. Le rapport vérifie 11 sources, 6 niveaux, 16 cas, 22 sondes et 49 métriques. Les mutations démontrent le blocage des divergences, preuves périmées et dépendances incohérentes. La garantie reste bornée aux autorités et versions déclarées. | — |
| RISK-004 | Percentiles, censures, `Risk Score` ou histogrammes incorrects. Critique car ces valeurs fondent la décision; faible dans les autorités déclarées, sans constituer un backtesting réel. | Engagement ou arbitrage fondé sur une probabilité fausse. | Critique | Faible | Censures, rangs, quantiles de survie, score, fiabilité et histogrammes sont testés dans les deux langages par corpus, rejeu exact et protocole multi-seeds. L’autorité sémantique surveille séparément chaque formule et la gate `main` bloque les mutations avant consolidation. | `docs/standards/STD-STAT-001.md`; `docs/statistical-reference-corpus.md`; `docs/statistical-compatibility.md`; `docs/statistical-main-enforcement.md`; `contracts/statistical-compatibility-authority-v1.0.json`; `Scripts/statistical_main_enforcement.py`; `reports/statistical-compatibility-evidence.json`; `tests/test_statistical_main_enforcement.py`; `tests/test_mc_core.py`; `frontend/src/utils/simulation.test.ts`; `tests/test_simulation_value_objects.py`; `frontend/src/utils/simulationRiskScore.test.ts` | Partiellement couvert | CP-003, CP-005 | Les seize cas et cinq scénarios concordent; les mutations de censure, percentile, score, fiabilité et histogramme sont bloquées. Le backtesting réel et la calibration temporelle restent ouverts. | PBI 9.4, 9.7 |
| RISK-005 | Contrat ou validation incorrecte de `POST /simulate`. Critique car des entrées invalides ou réponses incompatibles atteindraient le moteur; faible depuis l’alignement fermé des validations, mappers et versions statistiques. | Erreur serveur, calcul excessif ou rupture frontend/backend. | Critique | Faible | DTO et domaine séparés, conversions testées, entrées et sorties fermées, valeurs absentes omises, schéma et sondes interlangages, avec refus du corpus avant toute exécution moteur. Les validations, valeurs par défaut, forme canonique, sérialisation et historiques sont des surfaces versionnées avec décisions de migration ou d’invalidation, puis contrôlées dans `main`. | `docs/standards/STD-STAT-001.md`; `docs/statistical-compatibility.md`; `docs/statistical-main-enforcement.md`; `contracts/statistical-reference-corpus-v1.0.schema.json`; `contracts/statistical-compatibility-authority-v1.0.json`; `Scripts/run_statistical_reference_corpus.py`; `Scripts/run_statistical_compatibility.py`; `tests/test_statistical_compatibility.py`; `tests/test_statistical_corpus_runner.py`; `backend/api_models.py`; `backend/simulation_mappers.py`; `frontend/src/api/simulationMappers.test.ts` | Couvert | CP-003 | Le versionnement externe de l’API reste distinct de la compatibilité statistique contrôlée; les formules statistiques ne relèvent pas de ce risque de frontière. | PBI 7.2 |
| RISK-006 | Historique Azure DevOps incomplet ou temporellement incohérent. Critique car l'échantillon devient trompeur; moyenne car les API peuvent retourner lots, révisions ou dates partiels. | Prévision biaisée malgré un calcul correct. | Critique | Moyenne | Semaines complètes lundi-dimanche, avertissements sur collecte partielle et repli si révisions indisponibles. | `frontend/src/adoClient.ts`; `frontend/src/adoClient.test.ts`; `frontend/src/utils/cycleTime.ts`; `frontend/src/utils/cycleTime.test.ts` | Partiellement couvert | CP-004; candidat CP-009 | Les tests reposent sur réponses simulées; complétude, fuseaux, pagination et évolution des données réelles ne sont pas qualifiés globalement. | PBI 8.7, 8.8, 8.10 |
| RISK-007 | Incompatibilité Azure DevOps Cloud et Server/TFS. Élevé car elle bloque la collecte sur une plateforme; moyenne du fait des versions et topologies Server variées. | Onboarding ou historique indisponible pour certains déploiements. | Élevé | Moyenne | Détection Cloud/on-premise, URL de collection et `api-version=6.0` pour on-premise testées. | `frontend/src/adoPlatform.ts`; `frontend/src/adoPlatform.test.ts`; `frontend/src/adoClient.ts`; `frontend/src/adoClient.test.ts`; `frontend/src/hooks/useOnboarding.test.tsx` | Partiellement couvert | CP-004; candidat CP-009 | Aucun test d'intégration sur instances Cloud et Server/TFS réelles ni matrice de versions. | PBI 8.6, 8.11, 8.12 |
| RISK-008 | Résultat périmé ou restauré avec de mauvais paramètres. Élevé car l'interface pourrait attribuer une simulation à un autre scénario; moyenne car l'état frontend est complexe. | Décision prise sur une exécution différente de la configuration affichée. | Élevé | Moyenne | Signature de paramètres, invalidation et rejeu par seed; entrée incomplète ou différente non réutilisée. L’autorité de compatibilité inventorie caches, résultats seedés et rejeu, puis exige un traitement fermé pour toute dérive acceptée. | `frontend/src/utils/simulationSignature.ts`; `frontend/src/utils/simulationSignature.test.ts`; `frontend/src/hooks/useSimulation.ts`; `frontend/src/hooks/useSimulation.test.tsx`; `docs/statistical-compatibility.md`; `reports/statistical-compatibility-evidence.json` | Partiellement couvert | CP-005; candidat CP-006 | Aucune migration réelle n’était justifiée par l’adoption initiale; le cache n’a pas encore de versionnement complet ni d’E2E ciblé sur toutes les transitions de restauration. | PBI 10.3, 10.4 |
| RISK-009 | Incohérence entre interface et rapports. Critique car deux supports peuvent conduire à des arbitrages opposés; moyenne car les modèles de présentation restent multiples. | Décision ou communication contradictoire en comité. | Critique | Moyenne | Diagnostics partagés sur certaines vues et assertions sur libellés, percentiles, graphiques et rapport. | `frontend/src/utils/portfolioComparisonPresentation.ts`; `frontend/src/components/steps/SimulationResultsPanel.test.tsx`; `frontend/src/components/steps/simulationPrintReport.test.ts`; `frontend/src/components/steps/portfolioPrintReport.test.ts` | Partiellement couvert | CP-005 | Pas de modèle de données unique ni de comparaison E2E systématique UI/PDF. | PBI 10.5, 10.6, 10.9 |
| RISK-010 | Échec ou corruption de génération PDF. Critique car l'export est un point vital et peut être le support de décision; moyenne car le rendu dépend du DOM, de SVG et de bibliothèques navigateur. | Rapport absent, tronqué ou matériellement faux. | Critique | Moyenne | Génération simulation/portefeuille, pagination, absence de graphiques et repli de sauvegarde testés. | `frontend/src/components/steps/simulationPdfDownload.ts`; `frontend/src/components/steps/simulationExportModules.test.ts`; `frontend/src/components/steps/simulationPdfDownload.fallback.test.ts`; `frontend/src/components/steps/portfolioPrintReport.test.ts` | Partiellement couvert | CP-005 | Les tests mockent largement `jsPDF`; aucun parseur ne valide un fichier PDF réel ni son contenu rendu multi-plateforme. | PBI 10.7, 10.8 |
| RISK-011 | Indisponibilité ou incohérence de MongoDB. Élevé car l'historique disparaît mais le calcul principal peut continuer; moyenne pour une dépendance réseau. | Historique absent, 503 en lecture ou écriture perdue. | Élevé | Moyenne | Healthcheck, reconnexion unique, validation des lignes et poursuite du calcul si la persistance échoue. Le test Mongo réel échoue explicitement lorsque le service requis par le profil est absent. | `backend/simulation_store.py`; `tests/test_simulation_store.py`; `tests/test_api_history.py`; `tests/test_api_health.py` | Partiellement couvert | CP-003; candidat CP-007 | Peu de scénarios réels de panne/reprise, concurrence et corruption. | PBI 6.5, 11.4 |
| RISK-012 | Croissance non bornée du stockage MongoDB. Critique car une saturation peut rendre le service indisponible; élevée car le TTL porte sur `last_seen`, rafraîchi pour un client actif. | Disque saturé et perte de service ou de données. | Critique | Élevée | Index TTL de 30 jours et limite de lecture à 10 résultats. | `backend/simulation_store.py`; `tests/test_simulation_store.py`; `docs/backlog.md` | Partiellement couvert | Candidat CP-007 | La lecture limitée ne purge pas; l'activité d'un client prolonge toutes ses lignes. Le backlog identifie explicitement ce défaut. | PBI 4.1, 4.2 |
| RISK-013 | Contournement du rate limiting par adresse cliente falsifiée. Critique car un attaquant peut épuiser le calcul; élevée car le premier `X-Forwarded-For` est accepté sans proxy de confiance. | Déni de service et consommation CPU/mémoire. | Critique | Élevée | Limite par clé et test 429; extraction explicite du premier en-tête. | `backend/api_routes_simulate.py`; `tests/test_api_simulate.py`; `Dockerfile` | Partiellement couvert | CP-003 | Aucun modèle de proxies de confiance; le test caractérise précisément la valeur falsifiable. | PBI 4.5, 4.6 |
| RISK-014 | Conteneur exécuté avec des privilèges excessifs. Élevé car une compromission applicative aurait plus d'impact; élevée car aucune directive `USER` n'existe. | Élévation de l'impact d'une compromission du runtime. | Élevé | Élevée | Image runtime séparée du build frontend. | `Dockerfile`; `docs/backlog.md` | Non couvert | CP-003 | Le processus Python s'exécute avec l'utilisateur par défaut root. | PBI 4.4 |
| RISK-015 | Dépendances de développement dans l'image runtime. Élevé car elles élargissent surface et taille; élevée car le fichier runtime installe `pytest`, `pytest-cov`, `responses`, `httpx` et `ruff`. | Vulnérabilités et image plus lourde que nécessaire. | Élevé | Élevée | Build frontend multi-stage, mais dépendances Python non séparées. | `Dockerfile`; `requirements.txt`; `docs/backlog.md` | Non couvert | CP-003 | Aucun manifeste runtime minimal. | PBI 4.3 |
| RISK-016 | Saturation mémoire ou durée excessive des simulations. Critique car l'endpoint synchrone peut épuiser les workers; moyenne avec des bornes élevées jusqu'à 200 000 simulations. | Timeout, indisponibilité ou concurrence dégradée. | Critique | Moyenne | Bornes contractuelles, calcul par lots, horizon maximal et timeout serveur. | `backend/simulation_limits.py`; `backend/mc_core.py`; `backend/api_routes_simulate.py`; `tests/test_mc_core.py`; `tests/test_api_simulate.py` | Partiellement couvert | CP-003 | Aucun SLO ni test de charge/endurance; le thread de calcul expiré n'est pas démontré comme annulé. | PBI 6.8, 11.1, 11.2, 11.3, 11.5 |
| RISK-017 | Panne de Redis, MongoDB ou d'une autre dépendance. Élevé car les protections ou fonctions se dégradent; moyenne pour des services réseau. | Rate limiting permissif, historique indisponible ou démarrage dégradé. | Élevé | Moyenne | Journalisation et mode permissif Redis, health Mongo, reconnexion Mongo, healthchecks Compose. | `backend/api_routes_simulate.py`; `tests/test_api_simulate.py`; `tests/test_api_health.py`; `docker-compose.yml` | Partiellement couvert | CP-003; candidat CP-007 | Le repli Redis autorise les requêtes sans limitation partagée; pas de chaos, multi-worker ou reprise complète démontrée. | PBI 6.5, 11.4, 11.8 |
| RISK-018 | Défaut d'accessibilité d'un parcours essentiel. Élevé car certains utilisateurs seraient bloqués; moyenne en l'absence d'audit systématique. | Onboarding, simulation ou lecture des résultats inutilisable. | Élevé | Moyenne | Tests ponctuels des rôles, dialogues, `aria-live` et validation au clavier. | `frontend/src/components/steps/PatStep.test.jsx`; `frontend/src/components/steps/OrgStep.test.jsx`; `frontend/src/components/steps/TeamStep.test.jsx`; `frontend/src/components/steps/DecisionDiagnostic.test.tsx`; `frontend/src/components/steps/SimulationResultsPanel.test.tsx` | Partiellement couvert | CP-004, CP-005 | Aucun audit automatisé WCAG/axe, lecteur d'écran, contraste ou parcours clavier E2E complet. | PBI 6.6 |
| RISK-019 | Défaut de compatibilité plateforme ou navigateur. Élevé car un parcours peut échouer selon navigateur/OS; moyenne car les PDF, téléchargements et APIs varient. | Connexion, rendu ou téléchargement indisponible sur une cible. | Élevé | Moyenne | Vitest sous Windows/CI, E2E Playwright et repli de sauvegarde PDF. | `frontend/playwright.config.js`; `frontend/tests/e2e/onboarding.spec.js`; `frontend/src/components/steps/simulationPdfDownload.fallback.test.ts`; `.github/workflows/ci.yml` | Partiellement couvert | CP-004, CP-005 | Playwright n'énumère que son navigateur par défaut Chromium; aucune matrice Firefox/WebKit/OS ni Server/TFS réelle. | PBI 6.6 |
| RISK-020 | Défaillance silencieuse ou faux résultat vert de la chaîne qualité. Critique car toutes les autres preuves deviendraient non fiables; faible grâce aux validations d'artefacts et tests des gates. | Régression publiée malgré une validation affichée verte. | Critique | Faible | Gate centralisée, snapshot local en lecture seule, couvertures Python/frontend/E2E, fraîcheur des artefacts, Vitals à 95 %, ratchet, classification et gouvernance bloquante. Les attestations statistiques lient politique, snapshot, dépendances et empreintes ; l’agrégateur CI devient rouge sur tout nœud obligatoire échoué ou sauté hors `pr`. | `Scripts/quality_gate.py`; `Scripts/statistical_main_enforcement.py`; `config/statistical-main-enforcement-v1.0.json`; `.github/workflows/ci.yml`; `docs/statistical-main-enforcement.md`; `tests/test_statistical_main_enforcement.py`; `tests/test_statistical_main_enforcement_edges.py`; `tests/test_quality_gate.py`; `tests/test_test_strategy_reporting.py`; `tests/test_test_governance.py` | Partiellement couvert | Candidat CP-008 | Le dépôt documente le required check `aggregate`, mais ne peut démontrer la branch protection externe. La pertinence éditoriale du README reste une exigence de revue. | PBI 6.3 |

## Lecture de l'état réel

- Les risques critiques `RISK-001` et `RISK-002` disposent d'une maîtrise démontrée dans le périmètre actuel.
- Les autres risques critiques restent partiellement couverts; aucun n'est déclaré accepté formellement.
- `RISK-014` et `RISK-015` sont non couverts, mais d'impact élevé plutôt que critique selon la méthode ci-dessus.
- Aucun PBI existant n'a dû être inventé : toutes les références renvoient à [`backlog.md`](backlog.md).
